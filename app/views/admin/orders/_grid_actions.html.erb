<%= link_to '<span class="glyphicon glyphicon-edit"></span>'.html_safe,
            admin_order_path(v),
            :class => 'actions',
            :title => t('helpers.links.show', :model => Order.model_name.human) %>